# Scanlink remote-watcher env (SDRBOX side)
# Used by /etc/systemd/system/scanlink-remote.service

# Where the gateway is reachable from SDRBOX.
# (Gateway is the OpenClaw host running the webui + bot. Update if you move boxes.)
GATEWAY_URL=http://192.168.1.61:8080

# Where trunk-recorder writes recordings on this box.
RECORDINGS_DIR=/home/dadud/recordings/pittsville
